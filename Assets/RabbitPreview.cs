using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RabbitPreview : MonoBehaviour
{
    public GameObject preview; 
    // Start is called before the first frame update
    void Start()
    {
        preview.SetActive(true);
    }

   
}
